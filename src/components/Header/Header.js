// Header.js
import React from 'react';
import './Header.scss';

const Header = () => {
    return (
        <header className="header">
            <h1>My Matura Website</h1>
            {/* Dodaj inne elementy nagłówka, nawigację, itp. */}
        </header>
    );
};

export default Header;
