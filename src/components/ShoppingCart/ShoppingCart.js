// ShoppingCart.js
import React from 'react';
import './ShoppingCart.scss';

const ShoppingCart = () => {
    // Pobierz dane koszyka z kontekstu lub propów

    return (
        <div className="shopping-cart">
            <h2>Your Shopping Cart</h2>
            {/* Wyświetl listę produktów w koszyku, formularz, itp. */}
        </div>
    );
};

export default ShoppingCart;
